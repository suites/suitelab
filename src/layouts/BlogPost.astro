---
import type { CollectionEntry } from 'astro:content';
import Layout from './Layout.astro';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
import Bio from '@/components/Bio.astro';
import CategoryLabel from '@/components/CategoryLabel.astro';
import PostNavigation from '@/components/PostNavigation.astro';
import Disqus from '@/components/Disqus.astro';
import { formatDate } from '@/utils/date';
import { twemojiParse } from '@/utils/twemoji.util';
import { SITE_URL } from '@/consts';

type Props = CollectionEntry<'blog'>['data'] & {
  headings?: Array<{ depth: number; slug: string; text: string }>;
  readTime?: number;
};

const { title, description, pubDate, headings, readTime, ...otherProps } = Astro.props;

// 선택적 필드들 (우리 스타일 또는 다른 스타일 지원)
const date = 'date' in otherProps ? otherProps.date : null;
const category = 'category' in otherProps ? otherProps.category : null;
const emoji = 'emoji' in otherProps ? otherProps.emoji : null;

// 날짜 필드 통합 (기존 스타일과 템플릿 모두 지원)
const postDateValue = date || pubDate || new Date();
const postDate = formatDate(postDateValue);
const estimatedReadTime = readTime || 5;

// Disqus 설정 (Gatsby 버전과 동일)
const slug = ('slug' in otherProps ? otherProps.slug : null) || Astro.url.pathname;
const postUrl = `${SITE_URL}${slug}`;
const postId = Astro.url.pathname; // identifier로 사용
---

<Layout title={title} description={description || ''}>
  <Header />
  
  <main id="main-content" class="blog-post-layout">
    <div class="content-wrapper">
      <div class="content-grid">
        <!-- 메인 콘텐츠 -->
        <article class="post-content-wrapper">
          <section class="post-content-main">
          <!-- 히어로 이미지 (선인장 패턴 배경 + 이모지) -->
          {emoji && (
            <div class="hero-image" set:html={twemojiParse(emoji)} />
          )}
          
          <!-- 포스트 헤더 -->
          <header class="post-header">
            <div class="post-title-section">
              <h1 class="post-title">{title}</h1>
            </div>
            <div class="post-meta">
              {category && <CategoryLabel category={category} />}
              <time datetime={postDateValue.toISOString()} class="post-date">
                {postDate}
              </time>
              <span class="read-time" aria-label={`예상 읽기 시간 ${estimatedReadTime}분`}>
                📖 {estimatedReadTime}분 읽기
              </span>
            </div>
            
            {description && (
              <p class="post-description">{description}</p>
            )}
          </header>
        
          
          <!-- 포스트 본문 -->
          <div class="prose">
            <slot />
          </div>
          
          <!-- Disqus (RelatedPosts 위에 위치) -->
          <aside class="post-aside">
            <Disqus url={postUrl} identifier={postId} title={title} />
          </aside>
          
          <!-- 포스트 네비게이션 (RelatedPosts 역할) -->
          <PostNavigation />
          </section>
        </article>
        
        <!-- Bio -->
        <Bio />
      </div>
    </div>
  </main>
  
  <Footer />
</Layout>

<style>
  .blog-post-layout {
    padding: 0;
    min-height: 70vh;
  }
  
  /* content-wrapper는 global.css에서 통일 관리 */
  
  .content-grid {
    display: flex;
    align-items: flex-start;
    gap: 3rem;
    margin-top: 2em; /* Gatsby의 Content margin-top과 유사 */
  }
  
  .post-content-wrapper {
    flex: 1;
    min-width: 0; /* Flex overflow 방지 */
    max-width: 100%; /* 오버플로우 방지 */
  }

  .post-content-main {
    position: relative;
    background: #fff;
    overflow: hidden;
    font-size: 16px;
    color: #333;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0);
    width: 100%;
    max-width: 100%;
    box-sizing: border-box;
  }
  
  .post-content-main::before,
  .post-content-main::after {
    content: '';
    position: absolute;
    width: 0;
    height: 0;
    z-index: 5;
  }
  
  .post-content-main::before {
    top: 0;
    left: 0;
    border-right: 20px solid transparent;
    border-top: 20px solid rgb(33, 36, 45); /* theme.colors.background */
  }
  
  .post-content-main::after {
    bottom: 0;
    right: 0;
    border-bottom: 20px solid rgb(33, 36, 45); /* theme.colors.background */
    border-left: 20px solid transparent;
  }
  
  /* 히어로 이미지 (Gatsby 원본 스타일) */
  .hero-image {
    position: relative;
    background: #313746; /* theme.colors.blackLight */
    text-align: center;
    background-image: url('/images/svg/others/pattern.svg');
    background-repeat: repeat;
    background-size: 400px;
    min-height: 230px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0;
    padding: 0;
  }
  
  .hero-image :global(.emoji) {
    width: 110px !important;
    height: 110px !important;
  }
  
  .post-header {
    padding: 1.8em 2.5em 0; /* Gatsby와 동일: 1.8em 2.5em */
    margin-bottom: 0; /* Gatsby 버전에서는 ContentMain에 margin이 없고 PostContent에만 있음 */
  }
  
  .post-meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
  }
  
  .post-date {
    color: #999;
    font-size: 0.9rem;
    font-weight: 500;
  }
  
  .read-time {
    color: #999;
    font-size: 0.85rem;
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }
  
  .post-title-section {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
    margin-bottom: 1rem;
  }
  
  
  .post-title {
    font-size: clamp(1.8rem, 5vw, 2.5rem);
    font-weight: 700;
    line-height: 1.5;
    margin: 0.1rem 0 0.3rem;
    color: #333;
    word-break: keep-all;
  }
  
  .post-description {
    font-size: 1.1rem;
    color: #666;
    line-height: 1.6;
    margin: 0;
    font-style: italic;
  }
  
  .toc-mobile {
    display: none;
    margin-bottom: 2rem;
  }
  
  .sticky-element {
    position: sticky;
    top: 2rem;
  }
  
  .toc-desktop {
    display: block;
  }
  
  /* Post Aside - Gatsby 버전의 aside와 동일 */
  .post-aside {
    margin-top: 0;
  }
  
  /* Prose 스타일링 - Gatsby 원본 스타일 */
  .prose {
    max-width: none;
    color: #333;
    line-height: 1.9;
    font-size: 16px;
    margin: 1.5em 0 1em; /* Gatsby와 동일 */
    padding: 0 2.5em; /* Gatsby와 동일: ContentMain의 패딩과 동일한 좌우 패딩 */
    width: 100%;
    box-sizing: border-box;
    overflow-wrap: break-word;
    word-wrap: break-word;
  }
  
  .prose :global(h1),
  .prose :global(h2),
  .prose :global(h3),
  .prose :global(h4),
  .prose :global(h5),
  .prose :global(h6) {
    color: #333;
    font-weight: 700;
    line-height: 1.5;
    margin: 40px 0 10px;
    scroll-margin-top: 5rem;
  }
  
  .prose :global(h2) { 
    position: relative;
    margin: 55px 0 12px;
    padding: 5px 0 5px 17px;
    font-size: 1.55em;
  }
  
  .prose :global(h2)::before {
    position: absolute;
    top: 0;
    left: 0;
    content: '';
    display: inline-block;
    width: 5px;
    height: 100%;
    border-radius: 5px;
    background: linear-gradient(0deg, #ffa649 0%, #ffa649 50%, #f7645b 50%, #f7645b 100%);
  }
  
  .prose :global(h3) { 
    margin: 55px 0 12px;
    font-size: 1.45em; 
  }
  
  .prose :global(h4) { 
    font-size: 1.1em;
    position: relative;
    padding-left: 18px;
  }
  
  .prose :global(p) {
    margin-bottom: 1em; /* Gatsby와 동일: 1em */
  }
  
  .prose :global(a) {
    color: #0c9ee4;
    text-decoration: none;
    transition: all 0.2s ease;
  }
  
  .prose :global(a:hover) {
    text-decoration: underline;
  }
  
  .prose :global(blockquote) {
    margin: 2.3em 0; /* Gatsby와 동일: em 단위 */
    font-style: italic;
    background: #f1f4f7; /* theme.colors.whitesmoke - Gatsby와 동일 */
    padding: 1em; /* Gatsby와 동일: em 단위 */
  }
  
  .prose :global(blockquote p) {
    margin: 0.3em 0; /* Gatsby와 동일: em 단위 */
  }
  
  .prose :global(hr) {
    display: block;
    margin: 2em 0; /* Gatsby와 동일 */
    border: none;
    border-top: dotted 3px #e1eaf3; /* Gatsby와 동일 */
  }
  
  .prose :global(strong) {
    font-weight: 700; /* Gatsby와 동일 */
  }
  
  .prose :global(em) {
    font-style: italic; /* Gatsby와 동일 */
  }
  
  .prose :global(del) {
    text-decoration: line-through; /* Gatsby와 동일 */
  }
  
  .prose :global(code) {
    background: #f1f3f4;
    color: #e74c3c;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-family: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
    font-size: 0.9em;
    font-weight: 500;
  }
  
  .prose :global(pre) {
    background: #2d3748 !important;
    border-radius: 8px;
    padding: 1.5rem;
    margin: 2rem 0;
    overflow-x: auto;
    line-height: 1.6;
  }
  
  .prose :global(pre code) {
    background: none !important;
    color: inherit !important;
    padding: 0;
    border-radius: 0;
    font-size: 0.95rem;
  }
  
  .prose :global(img) {
    border-radius: 8px;
    margin: 1em 0; /* Gatsby와 동일: 1em */
    max-width: 100%;
    height: auto;
  }
  
  .prose :global(.gatsby-resp-image-wrapper) {
    margin: 1em 0; /* Gatsby와 동일 */
    box-shadow: 0 2px 5px -1px rgba(0, 0, 0, 0.25); /* Gatsby와 동일 */
    border-radius: 4px; /* Gatsby와 동일 */
    overflow: hidden;
  }
  
  .prose :global(ul),
  .prose :global(ol) {
    margin: 1em 0; /* Gatsby와 동일 */
  }
  
  .prose :global(ul) {
    padding-left: 1.2em; /* Gatsby와 동일 */
  }
  
  .prose :global(ul li) {
    margin: 0.4em 0; /* Gatsby와 동일 */
    list-style: disc;
  }
  
  .prose :global(ul ul) {
    margin: 0; /* Gatsby와 동일 */
  }
  
  .prose :global(ol) {
    counter-reset: number;
    padding-left: 0; /* Gatsby는 커스텀 카운터 사용 */
  }
  
  .prose :global(ol > li) {
    list-style: none;
    list-style-position: inside;
    position: relative;
    line-height: 25px;
    padding-left: 34px; /* Gatsby와 동일: 커스텀 카운터 공간 */
    margin: 1em 0;
  }
  
  .prose :global(ol > li::before) {
    display: inline-block;
    position: absolute;
    left: 0;
    top: 0;
    width: 25px;
    height: 25px;
    border-radius: 50%;
    background: #20a8ea; /* theme.colors.highlight */
    color: #fff;
    font-weight: 700;
    font-size: 14px;
    text-align: center;
    line-height: 25px;
    content: counter(number);
    counter-increment: number;
  }
  
  .prose :global(li) {
    line-height: 1.7;
  }
  
  .prose :global(ul p),
  .prose :global(ol p) {
    margin: 0; /* Gatsby와 동일 */
  }
  
  .prose :global(table) {
    display: block; /* Gatsby와 동일 */
    border-spacing: 2px;
    border-collapse: separate;
    overflow: auto;
    -webkit-overflow-scrolling: touch; /* Gatsby와 동일: 모바일 스크롤 개선 */
    width: auto; /* Gatsby와 동일 */
    font-size: 0.9em; /* Gatsby와 동일 */
    line-height: 1.4; /* Gatsby와 동일 */
    margin: 1.5em 0; /* Gatsby와 동일 */
  }
  
  .prose :global(th) {
    background-color: #20a8ea; /* theme.colors.highlight - Gatsby와 동일 */
    color: #fff;
    padding: 10px 12px; /* Gatsby와 동일 */
    text-align: center;
    font-weight: 700;
  }
  
  .prose :global(th[align='center']) {
    text-align: center;
  }
  
  .prose :global(th[align='right']) {
    text-align: right;
  }
  
  .prose :global(td) {
    background-color: #f1f4f7; /* theme.colors.whitesmoke - Gatsby와 동일 */
    color: #313746; /* theme.colors.blackLight - Gatsby와 동일 */
    padding: 10px 12px; /* Gatsby와 동일 */
  }
  
  .prose :global(thead tr th:first-child) {
    border-radius: 5px 0 0 0; /* Gatsby와 동일 */
  }
  
  .prose :global(thead tr th:last-child) {
    border-radius: 0 5px 0 0; /* Gatsby와 동일 */
  }
  
  .prose :global(tbody tr:last-child td:first-child) {
    border-radius: 0 0 0 5px; /* Gatsby와 동일 */
  }
  
  .prose :global(tbody tr:last-child td:last-child) {
    border-radius: 0 0 5px 0; /* Gatsby와 동일 */
  }
  
  /* 반응형 디자인 - Gatsby 브레이크포인트: small 500px, medium 768px, large 950px */
  @media screen and (max-width: 950px) {
    .content-grid {
      flex-direction: column;
      gap: 2rem;
    }
    
    .sticky-element {
      position: static;
    }
  }
  
  /* Gatsby의 small 브레이크포인트: 500px */
  @media screen and (max-width: 500px) {
    /* 모바일에서 content-wrapper의 padding 제거하여 화면 전체 사용 */
    .content-wrapper {
      padding: 0; /* BlogPost 전용: 모바일에서 패딩 제거 */
      max-width: 100%; /* BlogPost 전용: 모바일에서 최대 너비 제한 제거 */
    }
    
    .content-grid {
      margin-top: 0; /* Gatsby의 Content margin-top: 0과 동일 */
    }
    
    /* Gatsby와 동일: 작은 화면에서 화면 전체 사용 */
    .post-content-main {
      margin: 0; /* content-wrapper의 padding이 없으므로 음수 마진 불필요 */
    }
    
    /* Gatsby와 동일: 작은 화면에서 ::before, ::after 제거 */
    .post-content-main::before,
    .post-content-main::after {
      content: none;
    }
    
    /* Gatsby와 동일: 히어로 이미지 높이 축소 */
    .hero-image {
      min-height: 190px;
    }
    
    /* Gatsby와 동일: ContentMain 패딩 변경 */
    .post-header {
      padding: 30px 20px 0; /* theme.sideSpace.contentSmall */
    }
    
    .post-title {
      font-size: 25px; /* Gatsby와 동일 */
    }
    
    .post-meta {
      flex-direction: column;
      gap: 0.5rem;
      align-items: flex-start; /* 왼쪽 정렬 */
      text-align: left; /* 텍스트 왼쪽 정렬 */
    }
    
    .prose {
      margin: 1.5em 0 1em;
      padding: 0 20px; /* ContentMain의 패딩과 동일 */
    }
    
    .prose :global(pre) {
      margin-left: -20px; /* ContentMain의 음수 마진과 맞춤 */
      margin-right: -20px;
      border-radius: 0;
      padding: 1rem;
    }
  }
  
  /* 중간 크기 화면 (500px ~ 768px) */
  @media screen and (min-width: 501px) and (max-width: 768px) {
    .blog-post-layout {
      padding: 1rem 0;
    }
    
    .post-title-section {
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 1rem;
    }
    
    .post-meta {
      justify-content: flex-start;
      text-align: left;
    }
  }
</style>